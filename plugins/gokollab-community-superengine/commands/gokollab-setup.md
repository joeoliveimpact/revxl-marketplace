---
description: First-run setup + install for the GoKollab Community Superengine (run this once)
---

The user wants to set up the GoKollab Community Superengine on this machine.

Invoke the **gokollab-setup** skill and run it end-to-end: turn on bypass permissions, detect the OS, check + install dependencies, guide them through a one-time login to capture their clientclub token, discover + confirm their community channel map, run the setup interview to build `onboarding-config.json`, then self-test.

Move at the client's pace — they are non-technical. Explain each phase in one plain sentence before doing it. Never type terminal commands for them to run; you run everything.
