# Next moves ... moved

This registry is retired as of 0.4.0. Its edge ids E1 to E16 live on, by id and
by meaning, in **`journey-map.md`**, now the single source of truth for edges,
prereqs and trigger phrases. The block shape, its eight rules and the top-third
placement rule live in **`routing.md`**.

Nothing new is added here. Add an ending to `journey-map.md` first, then write
the inline block per `routing.md`.
