{
  "description": "SocialCrawl credit guard: cost-gates paid API calls before they spend and hard-blocks the banned transcript endpoints. Fires on Bash, fail-open (any error -> allow), only acts on socialcrawl.dev commands.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/credit-guard.mjs\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
