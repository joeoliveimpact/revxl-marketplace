<!-- GENERATED by tools/socialcrawl/gen_refs.py from research/socialcrawl-docs-2026-08-08/openapi.json.
     Do NOT hand-edit. Platform wisdom goes in tools/socialcrawl/curated/google_news.md; re-run to merge. -->

# Google News (1 endpoints)

## Endpoints

| Resource | Params | Credits | Description |
|----------|--------|---------|-------------|
| /search | `keyword` | 1 | Search Google News |

### Parameter legend

- `a`, `b` — both required.
- `a` / `b` — a `oneOf` group: at least one member must be provided.
- `a`? — the spec marks nothing required, but these are the parameters the endpoint accepts;
  a bare call usually will not work. See Endpoint Details for which combination to send.
- *(none)* — takes no parameters at all.
- A method prefix (**POST**, **DELETE**, **PATCH**) marks a non-GET endpoint; unmarked rows are GET.
- Every endpoint also accepts the `Cache-Control` header (send `no-cache` to bypass the
  response cache — billed at the normal rate) and `Idempotency-Key` (a UUID that makes a
  retry safe; a replay returns the cached payload without double-billing).

## Endpoint Details

_What each endpoint returns and when to reach for it._

### /search — 1cr

Returns a unified NewsArticleList of the top Google News results for a keyword, right now — headline, source outlet, thumbnail, article URL, and absolute UTC publish time (a text snippet may be present on some results). Ranked results land in one list (`placement` distinguishes a Top Stories block when present). The keyword supports quoted phrases and boolean operators (AND, OR, NOT). Localize with `location_name`/`location_code`/`location_coordinate` + `language_code` (non-Latin scripts supported). Filter by recency with `time_range`, by an exact date window with `from`/`to` (YYYY-MM-DD or Unix timestamp; overrides `time_range` when present), or by outlet with `publisher` (a bare domain like bbc.com). `publisher`/`from`/`to` cannot be combined with `location_coordinate` or `time_range=hour` (clear 400, never billed). There is no pagination beyond `depth` — Google News exposes at most 200 articles per query (default 10, max 100 on this surface). For the full article body, scrape the returned `url`. Served live from Google News — an ordinary synchronous request.

**Parameters**

- `keyword`: Search query (e.g. 'openai'). Supports any language; pass the matching language_code. Advanced operators (site:, intitle:, cache:, …) are not supported.
- `depth` *(optional)*: Number of articles to retrieve (default 10, multiples of 10, max 100). No pagination beyond depth.
- `location_code` *(optional)*: Google location code (e.g. 2840 = United States). Use one of location_code / location_name / location_coordinate.
- `location_name` *(optional)*: Google location name (e.g. 'South Korea').
- `location_coordinate` *(optional)*: GPS target as 'latitude,longitude,radius_mm' (e.g. '40.7128,-74.0060,200') for local-news radius queries.
- `language_code` *(optional)*: Google language code (e.g. 'en', 'ko'). Default 'en'.
- `time_range` *(optional)*: Only return articles published within this window: hour, day, week, month, or year. Ignored when from/to is present.
- `publisher` *(optional)*: Limit results to a single outlet's domain (e.g. 'bbc.com'). Bare domain only — no scheme, no path. Not combinable with location_coordinate or time_range=hour.
- `from` *(optional)*: Start of an exact publish-date window, as YYYY-MM-DD or a Unix timestamp. Overrides time_range. Not combinable with location_coordinate or time_range=hour.
- `to` *(optional)*: End of the publish-date window (inclusive), as YYYY-MM-DD or a Unix timestamp. Defaults to now when only from is set.

```bash
curl "https://www.socialcrawl.dev/v1/google_news/search?keyword=<keyword>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```


## What Google News is for

One endpoint, `/search` (1cr): news results for a query.

At 1 credit this is one of the cheapest rungs on the search ladder. Use it when the question
is "what is being reported about X" rather than "what are people saying about X" — for the
latter, `reddit/omni-search` (1cr) is the better 1cr call.

### Prefer `/v1/search/news` for anything multi-market

`/v1/search/news` (also 1cr) plans and fans one query across several Google News editions in a
single call, localized per market. Same price, broader coverage — so for anything that is not
explicitly single-market, reach for that instead.

For a brand's earned-media footprint with outlet rollup and dedupe, `prism/earned-media`
(25cr) is the composite version.
