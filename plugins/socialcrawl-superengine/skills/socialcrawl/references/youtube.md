<!-- GENERATED by tools/socialcrawl/gen_refs.py from research/socialcrawl-docs-2026-08-08/openapi.json.
     Do NOT hand-edit. Platform wisdom goes in tools/socialcrawl/curated/youtube.md; re-run to merge. -->

# Youtube (28 endpoints)

## Endpoints

| Resource | Params | Credits | Description |
|----------|--------|---------|-------------|
| /channel | `channelId` / `handle` / `url` | 1 | Get YouTube channel info |
| /channel/videos | `channelId` / `handle` | 1 | List YouTube channel videos |
| /video | `url` | 1 | Get YouTube video details |
| **POST** /videos | `ids` | 5 **/50-id chunk** | Batch get YouTube video details (up to 1000) |
| **POST** /channels | `ids` | 5 **/50-id chunk** | Batch get YouTube channel details (up to 1000) |
| **POST** /transcripts | `ids` | 3 **/successful video** | Up to 100 YouTube video ids → one transcript per row, failed ids refunded. |
| /video/sponsors | `url` | 10 | Detect sponsors of a YouTube video |
| /video/comments | `url` | 1 | List YouTube video comments |
| /video/comment/replies | `continuationToken` | 1 | List YouTube comment replies |
| /search | `query` | 1 | Search YouTube |
| /channel/shorts | `channelId` / `handle` | 1 | List YouTube channel shorts |
| /community-post | `url` | 1 | Get YouTube community post |
| /playlist | `playlist_id` | 1 | Get YouTube playlist |
| /search/hashtag | `hashtag` | 1 | Search YouTube by hashtag |
| /shorts/trending | *(none)* | 5 | Get trending YouTube shorts |
| /video/transcript | `url` | 3 | Get YouTube video transcript — ⛔ **BANNED in this plugin: 3 credits, no advantage. Transcribe with the local captions→Groq→Whisper chain instead.** |
| /channel/playlists | `channelId` / `handle` | 1 | List a YouTube channel's playlists |
| /channel/lives | `channelId` / `handle` | 1 | List a YouTube channel's live streams |
| /channel/community-posts | `channelId` / `handle` | 1 | List a YouTube channel's community posts |
| /videos/trending | `region`?, `category`?, `language`?, `max_results`?, … | 1 | Get trending YouTube videos |
| /playlist/items | `playlist_id` | 1 | List the videos in a YouTube playlist |
| /search/advanced | `query` | 1 | Advanced YouTube video search |
| /search/suggestions | `query` | 1 | Get YouTube search suggestions |
| /video/audio | `url` | 5 | Get a YouTube video's audio file streams |
| /video/files | `url` | 5 | Get a YouTube video's video file streams |
| /video/subtitles | `url` | 1 | Get a YouTube video's subtitle files |
| /video/thumbnails | `url` | 1 | Get a YouTube video's thumbnail files |
| /profile/full | `handle`?, `channelId`?, `url`?, `posts`?, … | 5 | YouTube profile, recent posts, and computed analytics in one call. |

### Parameter legend

- `a`, `b` — both required.
- `a` / `b` — a `oneOf` group: at least one member must be provided.
- `a`? — the spec marks nothing required, but these are the parameters the endpoint accepts;
  a bare call usually will not work. See Endpoint Details for which combination to send.
- *(none)* — takes no parameters at all.
- A method prefix (**POST**, **DELETE**, **PATCH**) marks a non-GET endpoint; unmarked rows are GET.
- Every endpoint also accepts the `Cache-Control` header (send `no-cache` to bypass the
  response cache — billed at the normal rate) and `Idempotency-Key` (a UUID that makes a
  retry safe; a replay returns the cached payload without double-billing).

## Endpoint Details

_What each endpoint returns and when to reach for it._

### /channel — 1cr

Returns public channel information for a YouTube channel including subscriber count, total video count, total view count, channel description, banner URL, and avatar URL. `author.followers` is YouTube's publicly published subscriber figure. Above 1,000 subscribers YouTube rounds that figure to three significant figures, and `author.ext.followers_approximate` is true.

**Parameters**

- `hl` *(optional)*: Preferred response language for localized text (ISO 639-1, e.g. 'en', 'es', 'fr').
- `forUsername` *(optional)*: Legacy YouTube username (pre-handle) to look up.
- `channelId` / `handle` / `url` — at least one required.
  - `channelId`: YouTube channel ID. Can pass a channelId, handle or url (one of: channelId, handle, url — at least one required)
  - `handle`: YouTube channel handle without the @ symbol (one of: channelId, handle, url — at least one required)
  - `url`: YouTube channel URL. Can pass a channelId, handle or url (one of: channelId, handle, url — at least one required)

```bash
curl "https://www.socialcrawl.dev/v1/youtube/channel?channelId=<channelId>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /channel/videos — 1cr

Returns a list of recent videos published by a YouTube channel. Each video includes title, view count, like count, comment count, duration, thumbnail URL, and publish date. SHORTS: this reads the channel's Videos tab, which is long-form only — Shorts are a separate shelf on YouTube and never appear here (measured 0 of 30 items on 2 channels, 2026-08-11). For a channel's Shorts call `/v1/youtube/channel/shorts` (1 credit, same `channelId`/`handle`); the two sets are disjoint, so calling both and concatenating gives the channel's full catalogue for 2 credits. Every item on both endpoints carries `post.ext.content_type` (`video` or `short`) so the merged list stays separable.

**Parameters**

- `sort` *(optional)*: Sort by latest or popular
- `continuationToken` *(optional)*: Continuation token to get more videos. Get 'continuationToken' from previous response. Native upstream cursor param. Send the universal `cursor` instead — the API maps it to this name for you.
- `includeExtras` *(optional)*: Set to `true` to add the like count and comment count (`post.engagement.likes` / `.comments`) and the video description (`post.ext.description`). For the full per-video detail use /v1/youtube/video. Slows the response slightly.
- `is_paid_promotions` *(optional)*: Set to 'true' to search YouTube's public paid product placement / sponsorship / endorsement surface — returns normal videos where the creator disclosed a paid promotion.
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.
- `channelId` / `handle` — at least one required.
  - `channelId`: YouTube channel ID (one of: channelId, handle — at least one required)
  - `handle`: YouTube channel handle without the @ symbol (one of: channelId, handle — at least one required)

```bash
curl "https://www.socialcrawl.dev/v1/youtube/channel/videos?channelId=<channelId>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /video — 1cr

Returns detailed information about a specific YouTube video including title, view count, like count, comment count, description, tags, duration, channel info, and publish date.

**Parameters**

- `url`: Full URL of the YouTube video
- `language` *(optional)*: Preferred response language (mapped to Accept-Language header; not guaranteed due to YouTube localization behavior). 2 letter language code, ie 'en', 'es', 'fr' etc.
- `hl` *(optional)*: Preferred response language for localized text (ISO 639-1, e.g. 'en', 'es', 'fr').

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### **POST** /videos — 5cr per 50-id chunk

Fetches full details for up to 1000 YouTube videos by id in a single POST request. Each item is the same unified video object as GET /v1/youtube/video. Body: { ids: string[], includeLocalizations?: boolean }. Metered per 50-id chunk; a request that resolves no videos at all returns 200 with an empty list at 0 credits.

> ⚠️ **Metered per 50-id chunk — the 5cr figure is a UNIT price, not the price of the request.** 1000 ids = 20 chunks = 100cr. Multiply by the number of 50-id chunks you send before calling; only successful units are charged.

**Parameters**

- `hl` *(optional)*: Preferred response language for localized text (ISO 639-1, e.g. 'en', 'ko').
- `ids` *(body)*: 
- `include_localizations` *(body, optional)*: 

```bash
curl -X POST "https://www.socialcrawl.dev/v1/youtube/videos" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY" \
  -H "content-type: application/json" \
  -d '{"ids": "<ids>"}'
```

### **POST** /channels — 5cr per 50-id chunk

Fetches full details for up to 1000 YouTube channels by id in a single POST request. Each item is the unified channel (author) object. Body: { ids: string[], includeLocalizations?: boolean }. Metered per 50-id chunk; a request that resolves no channels at all returns 200 with an empty list at 0 credits.

> ⚠️ **Metered per 50-id chunk — the 5cr figure is a UNIT price, not the price of the request.** 1000 ids = 20 chunks = 100cr. Multiply by the number of 50-id chunks you send before calling; only successful units are charged.

**Parameters**

- `hl` *(optional)*: Preferred response language for localized text (ISO 639-1, e.g. 'en', 'ko').
- `ids` *(body)*: 
- `include_localizations` *(body, optional)*: 

```bash
curl -X POST "https://www.socialcrawl.dev/v1/youtube/channels" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY" \
  -H "content-type: application/json" \
  -d '{"ids": "<ids>"}'
```

### **POST** /transcripts — 3cr per successful video

Batch transcript lookup for LLM corpora. POST a JSON body with an `ids` array of 1–100 BARE 11-character YouTube video ids (full watch URLs are not accepted here — use GET /v1/youtube/video/transcript for a single URL); returns one row per id — the transcript (`format: "text"` joins it into one string; `format: "segments"` returns `[{start_ms, duration_ms, text}]`), the resolved `language`, and a `status` of ok / not_found / error / deferred — in input order. Optional `language` picks a preferred caption track. Per-id isolation: one caption-less or deleted video never fails the batch — a caption-less video is `not_found` with `ext.reason: "no_captions"`, a deleted/unavailable one is `not_found` with `ext.reason: "video_gone"`. Billing is per successful row at 3 credits (the single-transcript registry tier); not_found / errored / deferred rows are refunded, so you pay exactly for the transcripts you got. Never cached. Optionally streams Server-Sent Events when the client sends `Accept: text/event-stream`.

> ⚠️ **Metered per successful video — the 3cr figure is a UNIT price, not the price of the request.** verified live 08.08.26: 1 row = 3cr. Multiply by the number of successful videos you send before calling; only successful units are charged.

**Parameters**

- `ids` *(body)*: 
- `language` *(body, optional)*: 
- `format` *(body, optional)*: 

```bash
curl -X POST "https://www.socialcrawl.dev/v1/youtube/transcripts" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY" \
  -H "content-type: application/json" \
  -d '{"ids": "<ids>"}'
```

### /video/sponsors — 10cr

Checks a YouTube video for the paid-promotion disclosure and infers likely sponsors/promoted brands from the public description, description links, promo-code text, and transcript. Experimental upstream — sponsor attribution is inferred, with per-sponsor confidence and evidence.

**Parameters**

- `url`: Full URL of the YouTube video or short
- `language` *(optional)*: 2 letter language code used for transcript lookup, ie 'en', 'es', 'fr' etc.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video/sponsors?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /video/comments — 1cr

Returns a list of comments on a specific YouTube video. Each comment includes the author name, comment text, like count, reply count, and publish timestamp.

**Parameters**

- `url`: Full URL of the YouTube video to fetch comments for
- `continuationToken` *(optional)*: Continuation token to get more comments. Get 'continuationToken' from previous response. Native upstream cursor param. Send the universal `cursor` instead — the API maps it to this name for you.
- `order` *(optional)*: Order of comments
- `searchTerm` *(optional)*: Filter comments to those containing this search term.
- `format` *(optional)*: Text format for comment bodies — 'html' (default) or 'plainText'.
- `max_results` *(optional)*: Maximum number of comments to return per page.
- `channel_id` *(optional)*: YouTube channel id to fetch channel-level community comments for (instead of a video's comments).
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video/comments?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /video/comment/replies — 1cr

Fetches replies to a specific comment on a YouTube video. Each reply includes text content, author details, like count, and publish date. Requires a continuationToken from the Comments endpoint. YouTube's comment model is two levels deep: a top-level comment and one flat list of replies. There is no reply-to-a-reply thread, so reply objects carry no continuation token of their own. When someone answers another reply, YouTube stores it in this same flat list (usually prefixed with an @mention), so paginating this endpoint to exhaustion returns the complete tree. Paginate with the pagination.has_more flag rather than the reply count on the parent comment: that count is YouTube's own tally and can include replies withheld from the public list (held for review, spam-filtered, or from deleted accounts), so an exhausted list is often shorter than it.

**Parameters**

- `continuationToken`: Continuation token for the comment replies. Use 'repliesContinuationToken' from the Comments endpoint, or 'continuationToken' from a previous replies response to paginate.
- `format` *(optional)*: Text format for reply bodies — 'html' (default) or 'plainText'.
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video/comment/replies?continuationToken=<continuationToken>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /search — 1cr

Searches YouTube by keyword. Returns matching videos, shorts, and live streams (an untyped search returns all three; `type` scopes to one kind, including channels and playlists). Each video result includes title, URL, thumbnail, view count, publish date, duration, channel info, and badges. For Shorts results, exact publish dates and durations require `includeExtras=true`.

**Parameters**

- `query`: Search keyword or phrase.
- `uploadDate` *(optional)*: Upload date filter. Reliable on its own (no `type`). With `type=shorts`, windows narrower than `this_year` are rejected with a 400 because YouTube returns an empty set for them; `this_year` is accepted but measured to return ~5x fewer unique Shorts across a paginated walk (the upstream re-serves the same items under an advancing `continuationToken`), so the recommended pattern for Shorts recency is no `uploadDate` at all plus `includeExtras=true` and a client-side filter on `post.published_at`. With other `type` values the upstream currently ignores the date filter and may mix content kinds; filter client-side there too. For an EXACT date window rather than these coarse buckets, use `/v1/youtube/search/advanced`, whose `published_after`/`published_before` take RFC-3339 bounds and are genuinely applied.
- `sortBy` *(optional)*: Sort order — relevance or popular. `popular` cannot be combined with `type=shorts` (rejected with a 400 — YouTube serves that combination unreliably); it also caps the page at ~20 results. It is also NOT a strict view-count sort: `popular` is a popularity-weighted ranking, and results do not come back in descending view order. Measured on 4 of 4 test keywords — a `new york` search returned 9.5M, then 502.9M, then 66K views in that order. If you need a view-faithful ordering, use `/v1/youtube/search/advanced` with `order=viewCount` and `includeExtras=true` — its ranking leads with the true top results, and the returned `engagement.views` lets you sort the page exactly client-side.
- `type` *(optional)*: Type of content to return. NOTE for `type=shorts`: the Shorts shelf item carries NO channel object upstream, so every Shorts result has `post.author.*` and `post.ext.channel_id` null — measured 0 of 98 items across 3 keywords (2026-08-10), on the raw upstream too, and `includeExtras=true` does not add them. Video results are unaffected (20/20 carry both). To attach a creator to Shorts results, take the `post.id` values and call `POST /v1/youtube/videos` (5 credits per 50 ids), which returns `post.ext.channel_id` for every one; `/v1/youtube/search/advanced` also carries `channel_id` on every result at 1 credit, though it has no Shorts filter.
- `duration` *(optional)*: Video duration filter. KNOWN UPSTREAM ISSUE: currently not applied by the provider and it can degrade the `type` filter; prefer filtering client-side on `post.content.duration_seconds`.
- `region` *(optional)*: 2-letter country code of the country to put the proxy in.
- `continuationToken` *(optional)*: Continuation token to get more results. Get `continuationToken` from a previous response. Native upstream cursor param. Send the universal `cursor` instead — the API maps it to this name for you.
- `includeExtras` *(optional)*: Set to `true` to add the like count and comment count (`post.engagement.likes` / `.comments`) and the video description (`post.ext.description`) to each video result. Left off, those three are null. For Shorts results it also supplies `post.published_at` and `post.content.duration_seconds` — Shorts items carry neither without it, so recency filtering over `type=shorts` requires this flag. Slows the response slightly. It does NOT add channel identity to Shorts results (see `type`) and it never adds a subscriber count: no search surface on any platform returns one. For the channel's subscriber count take `post.ext.channel_id` and call `GET /v1/youtube/channel?channelId=` (1 credit, `author.followers`) or batch it with `POST /v1/youtube/channels` (up to 1000 ids, 5 credits per 50). For full per-video details use `/v1/youtube/video`.
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/search?query=<query>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /channel/shorts — 1cr

Returns a list of YouTube Shorts published by a channel, read from the channel's own Shorts shelf. Each short includes view count, like count, comment count, title, thumbnail, duration and exact publish date (measured 100% coverage on all six across 96 items, 2026-08-11), and unlike `/v1/youtube/search?type=shorts` every item carries channel identity. This is the Shorts complement to `/v1/youtube/channel/videos`, which is long-form only: the two sets are disjoint, so calling both and concatenating gives the channel's full catalogue for 2 credits. Every item carries `post.ext.content_type` (`short` here, `video` there) so the merged list stays separable.

**Parameters**

- `sort` *(optional)*: Sort by newest (descending publish date) or popular (descending view count). Both orderings are genuinely applied, unlike `/v1/youtube/search`'s `popular`, which is a popularity-weighted ranking rather than a view sort.
- `continuationToken` *(optional)*: Continuation token to get more videos. Get 'continuationToken' from previous response. Native upstream cursor param. Send the universal `cursor` instead — the API maps it to this name for you.
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.
- `channelId` / `handle` — at least one required.
  - `channelId`: Can pass channelId or handle (one of: channelId, handle — at least one required)
  - `handle`: YouTube channel handle without the @ symbol (one of: channelId, handle — at least one required)

```bash
curl "https://www.socialcrawl.dev/v1/youtube/channel/shorts?channelId=<channelId>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /community-post — 1cr

Returns details of a YouTube community post including text content, like count, comment count, images, and author info.

**Parameters**

- `url`: Full URL of the YouTube community post

```bash
curl "https://www.socialcrawl.dev/v1/youtube/community-post?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /playlist — 1cr

Returns videos in a YouTube playlist including titles, view counts, durations, thumbnails, and channel info.

**Parameters**

- `playlist_id`: YouTube playlist ID
- `cursor` *(optional)*: Pagination cursor from a previous response — fetches the next page.
- `channel_id` *(optional)*: YouTube channel id — pages that channel's uploads instead of a playlist.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/playlist?playlist_id=<playlist_id>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /search/hashtag — 1cr

Searches YouTube for videos under a specific hashtag. Returns matching videos with view counts, channel info, and publish dates.

**Parameters**

- `hashtag`: Hashtag to search for without the # symbol
- `continuationToken` *(optional)*: Continuation token to get more videos. Get 'continuationToken' from previous response. Native upstream cursor param. Send the universal `cursor` instead — the API maps it to this name for you.
- `type` *(optional)*: Search for all types of content or only shorts
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/search/hashtag?hashtag=<hashtag>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /shorts/trending — 5cr

Returns currently trending YouTube Shorts with view counts, like counts, channel info, and thumbnails.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/shorts/trending" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /video/transcript — 3cr

Returns the transcript of a YouTube video as timestamped segments — each with text, start `offset` and `duration` (seconds), language, word count, and speech rate. Supports multiple languages via `language`. When no transcript exists (the owner disabled captions, the video is login-gated or gone, or no caption track matches the requested language) the endpoint returns 404 RESOURCE_NOT_FOUND with `error.details.reason` set to `captions_disabled`, `login_required`, `no_captions`, or `video_gone` — no credits are charged, and retrying returns the same result.

> ⛔ **BANNED in this plugin: 3 credits, no advantage. Transcribe with the local captions→Groq→Whisper chain instead.**

**Parameters**

- `url`: Full URL of the YouTube video
- `language` *(optional)*: 2 letter language code, ie 'en', 'es', 'fr' etc. If no caption track matches the language you specify, the request returns 404 RESOURCE_NOT_FOUND (reason `no_captions`) at 0 credits.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video/transcript?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /channel/playlists — 1cr

Fetches playlists from a YouTube channel's Playlists tab — playlist ID, title, thumbnail, video count, channel info, and playlist URL — plus a `continuationToken` when more results are available. Pass `handle` or `channelId` for the first page; forward `continuationToken` for subsequent pages.

**Parameters**

- `continuationToken` *(optional)*: Continuation token from a previous response — fetches the next page. Native upstream cursor param. Send the universal `cursor` instead — the API maps it to this name for you.
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.
- `channelId` / `handle` — at least one required.
  - `channelId`: YouTube channel ID. (one of: channelId, handle — at least one required)
  - `handle`: YouTube channel handle (with or without @). (one of: channelId, handle — at least one required)

```bash
curl "https://www.socialcrawl.dev/v1/youtube/channel/playlists?channelId=<channelId>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /channel/lives — 1cr

Fetches live streams and past streams from a YouTube channel's Live tab — title, URL, thumbnail, view count, publish time, duration — plus a `continuationToken` when more results are available. Pass `handle` or `channelId` for the first page; forward `continuationToken` for subsequent pages.

**Parameters**

- `continuationToken` *(optional)*: Continuation token from a previous response — fetches the next page. Native upstream cursor param. Send the universal `cursor` instead — the API maps it to this name for you.
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.
- `channelId` / `handle` — at least one required.
  - `channelId`: YouTube channel ID. (one of: channelId, handle — at least one required)
  - `handle`: YouTube channel handle (with or without @). (one of: channelId, handle — at least one required)

```bash
curl "https://www.socialcrawl.dev/v1/youtube/channel/lives?channelId=<channelId>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /channel/community-posts — 1cr

Fetches community posts from a YouTube channel's Posts tab — post ID, URL, content, images, attached video, like count, publish time, channel info — plus a `continuationToken` when more results are available. Pass `handle` or `channelId` for the first page; forward `continuationToken` for subsequent pages.

**Parameters**

- `continuationToken` *(optional)*: Continuation token from a previous response — fetches the next page. Native upstream cursor param. Send the universal `cursor` instead — the API maps it to this name for you.
- `cursor` *(optional)*: Universal pagination cursor. Send `pagination.next_cursor` from the previous response back verbatim — the API maps it to this endpoint's native `continuationToken` (cursor style). You never construct, decode, or look up a cursor. Omit it for page 1.
- `channelId` / `handle` — at least one required.
  - `channelId`: YouTube channel ID. (one of: channelId, handle — at least one required)
  - `handle`: YouTube channel handle (with or without @). (one of: channelId, handle — at least one required)

```bash
curl "https://www.socialcrawl.dev/v1/youtube/channel/community-posts?channelId=<channelId>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /videos/trending — 1cr

Returns the most popular (trending) YouTube videos for a region and category — title, thumbnail, duration, view/like/comment counts, channel, and publish time — plus a `cursor` when more results are available. Filter by `region` (ISO country) and `category` (YouTube category id); forward `cursor` for subsequent pages.

**Parameters**

- `region` *(optional)*: ISO 3166-1 alpha-2 country code (e.g. US, GB, KR).
- `category` *(optional)*: YouTube video category id (e.g. 10 = Music, 24 = Entertainment).
- `language` *(optional)*: Localization language (ISO 639-1) for titles/metadata.
- `max_results` *(optional)*: Maximum number of videos to return (1–50).
- `cursor` *(optional)*: Pagination cursor from a previous response — fetches the next page.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/videos/trending" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /playlist/items — 1cr

Returns the videos in a YouTube playlist in order — video id, title, thumbnail, owning channel, and publish time — plus a `cursor` when more results are available. Pass `playlist_id` for the first page; forward `cursor` for subsequent pages.

**Parameters**

- `playlist_id`: YouTube playlist id (the value after `list=` in a playlist URL).
- `cursor` *(optional)*: Pagination cursor from a previous response — fetches the next page.
- `channel_id` *(optional)*: YouTube channel id — pages that channel's uploads instead of a playlist.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/playlist/items?playlist_id=<playlist_id>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /search/advanced — 1cr

Searches YouTube videos with the full filter set — sort `order`, `duration`, live/upcoming `event_type`, Creative-Commons `license`, `category`, `region`, `language`, and `published_after`/`published_before` date windows — returning video id, title, thumbnail, channel, and publish time, plus a `cursor` for the next page. Results are always videos. This is the reliable lane for date-window, sort, and page-size work. `published_after`/`published_before` take exact RFC-3339 bounds and are genuinely applied (`/v1/youtube/search`'s coarse `uploadDate` buckets are silently dropped by that upstream whenever a `type` is also set); `order=date` is an exact sort (measured: zero inversions); `order=viewCount` is YouTube's popularity-weighted ranking — strongly view-correlated with the top result leading, but not strictly descending, so with `includeExtras=true` sort the page client-side on `engagement.views` for exact ranking (the counts are in the response); and `max_results` returns up to the page size you ask for, 1–50 (exact when YouTube has the supply). Every result carries an exact per-second `published_at`. Set `includeExtras=true` to add view/like/comment counts and `duration_seconds` to every result (+5 credits per page) — without it those are null, because YouTube's search index returns snippets only. SHORTS: there is no `type=shorts` here and there cannot be — YouTube's Data API has no Shorts concept, so no filter can distinguish a vertical Short from any other video. The documented APPROXIMATION is `duration=short` (under 4 minutes) plus `includeExtras=true` and a client-side filter on `post.content.duration_seconds <= 180`; it will include short landscape videos, so treat it as a narrowing heuristic, not a Shorts filter. For a real Shorts shelf use `/v1/youtube/search?type=shorts`, which reads YouTube's own Shorts surface and carries its own caveats.

**Parameters**

- `query`: Search query.
- `order` *(optional)*: Sort order: date, rating, relevance (default), title, videoCount, viewCount. `order=date` is exact (measured: zero inversions against the timestamps). `order=viewCount` is YouTube's popularity-weighted ranking — the top result is the true maximum but mid-list order is approximate, so for exact ranking add `includeExtras=true` and sort the page on `engagement.views` client-side. Both are far more faithful than `/v1/youtube/search`'s `sortBy=popular`, which is not view-ordered at all.
- `duration` *(optional)*: Video length: short (<4m), medium (4–20m), long (>20m), any. Genuinely applied (unlike `/v1/youtube/search`'s `duration`, which the upstream ignores). `short` plus a client-side `post.content.duration_seconds <= 180` filter is the documented Shorts approximation — see the endpoint description for why it is an approximation and not a Shorts filter.
- `event_type` *(optional)*: Broadcast type: live, upcoming, completed.
- `license` *(optional)*: License filter: creativeCommon, youtube, any.
- `category` *(optional)*: YouTube video category id (e.g. 10 = Music).
- `region` *(optional)*: ISO 3166-1 alpha-2 country code.
- `language` *(optional)*: Preferred result language (ISO 639-1).
- `published_after` *(optional)*: RFC-3339 datetime lower bound (e.g. 2026-01-01T00:00:00Z). Exact and genuinely applied — this is the endpoint to use for date-window work.
- `published_before` *(optional)*: RFC-3339 datetime upper bound. Exact and genuinely applied.
- `channel_id` *(optional)*: Restrict results to a single channel id.
- `max_results` *(optional)*: Maximum number of videos to return (1–50). Honored exactly — a page of 50 returns 50 (live-verified).
- `cursor` *(optional)*: Pagination cursor from a previous response — fetches the next page.
- `safe_search` *(optional)*: Safe-search filter: none, moderate, strict.
- `video_caption` *(optional)*: Caption filter: any, closedCaption, none.
- `video_definition` *(optional)*: Quality filter: any, high, standard.
- `video_dimension` *(optional)*: Dimension filter: 2d, 3d, any.
- `video_embeddable` *(optional)*: Restrict to embeddable videos: true, any.
- `video_type` *(optional)*: Type filter: any, episode, movie.
- `topic_id` *(optional)*: Restrict to a Freebase topic id (e.g. /m/04rlf for music).
- `location` *(optional)*: Latitude,longitude center for a geo search (e.g. 37.42307,-122.08427). Must be used together with location_radius.
- `location_radius` *(optional)*: Radius around location with a unit suffix (e.g. 50km, 10mi). Must be used together with location.
- `includeExtras` *(optional)*: Set to `true` to add the view, like and comment counts (`post.engagement.views` / `.likes` / `.comments`) and the video length (`post.content.duration_seconds`) to every result. Left off, those four are null — YouTube's search index returns snippets only, so the counts come from a second lookup. Costs +5 credits per page (a page is at most 50 results, so it is a flat +5, not per result) and adds one upstream round-trip. If that lookup fails the page still returns, un-hydrated, with the +5 refunded and `_warnings: ["extras_unavailable"]` on the response.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/search/advanced?query=<query>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /search/suggestions — 1cr

Returns YouTube's search autocomplete suggestions for a partial query — the same suggestions the search box shows. Useful for keyword expansion and SEO research. Returns a string list under `items`.

**Parameters**

- `query`: Partial search query to autocomplete.
- `region` *(optional)*: ISO 3166-1 alpha-2 country code to localize suggestions.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/search/suggestions?query=<query>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /video/audio — 5cr

Returns the downloadable audio stream files for a YouTube video — each with a direct media `url`, mime type, bitrate, audio quality, sample rate, channels, and approximate duration. The stream URLs are time-limited: fetch them immediately and do not cache.

**Parameters**

- `url`: Full URL of the YouTube video.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video/audio?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /video/files — 5cr

Returns the downloadable video stream files for a YouTube video — each with a direct media `url`, mime type, resolution (`width`/`height`), quality label, fps, bitrate, and approximate duration. The stream URLs are time-limited: fetch them immediately and do not cache.

**Parameters**

- `url`: Full URL of the YouTube video.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video/files?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /video/subtitles — 1cr

Returns the downloadable subtitle/caption track files for a YouTube video — each with a language code and name, format, and a direct download `url`. Includes both manual and auto-generated tracks across available subtitle formats (srt, vtt, ttml, …).

**Parameters**

- `url`: Full URL of the YouTube video.
- `format` *(optional)*: Subtitle file format filter (e.g. srt, vtt, ttml, json3, srv1).

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video/subtitles?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /video/thumbnails — 1cr

Returns the thumbnail image files for a YouTube video at every available size — each with a direct image `url`, dimensions (`width`/`height`), aspect `ratio`, and image `format` (webp / jpg).

**Parameters**

- `url`: Full URL of the YouTube video.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/video/thumbnails?url=<url>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /profile/full — 5cr

Fans out to the YouTube profile and recent-posts endpoints in parallel and returns the unified author, the recent-post list, and computed metrics — average engagement rate, posting cadence (with the window it was measured over), the top post, and the format mix. The profile leg is the only critical leg: if posts can't be fetched the call still returns the profile with post-dependent metrics null, and every leg's status is surfaced in legs[]. Flat 5 credits.

**Parameters**

- `handle` *(optional)*: handle
- `channelId` *(optional)*: channelId
- `url` *(optional)*: url
- `posts` *(optional)*: How many recent posts to fetch + average the computed metrics over (1–100, default 25).
- `cursor` *(optional)*: Pass a prior response's posts_cursor to deepen the post window.
- `include` *(optional)*: CSV subset of posts,computed (default both). include=computed drops the raw posts[] to save payload.

```bash
curl "https://www.socialcrawl.dev/v1/youtube/profile/full" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```


## Choosing a YouTube endpoint

| Your goal | Use this | Credits |
|-----------|----------|---------|
| Channel info / its videos / one video | `/channel` · `/channel/videos` · `/video` | 1 |
| Shorts, lives, community posts, playlists | `/channel/shorts` · `/channel/lives` · `/channel/community-posts` · `/channel/playlists` | 1 |
| Search, with filters | `/search` · `/search/advanced` | 1 |
| What people type into YouTube | `/search/suggestions` | 1 |
| Comments, and replies | `/video/comments` · `/video/comment/replies` | 1 |
| What's trending | `/videos/trending` | 1 |
| Captions (the cheap transcript route) | `/video/subtitles` | 1 |
| Thumbnails | `/video/thumbnails` | 1 |
| Trending shorts | `/shorts/trending` | 5 |
| Audio / video file streams | `/video/audio` · `/video/files` | 5 |
| Profile + posts + analytics in one call | `/profile/full` | 5 |
| **Up to 1000 videos or channels at once** | `POST /videos` · `POST /channels` | 5 **per 50-id chunk** |
| Who sponsored this video | `/video/sponsors` | **10** |
| ~~Transcript~~ | ⛔ `/video/transcript` is **banned** — see below | — |

### Batch before you loop — but know what the unit is

`POST /videos` and `POST /channels` take up to 1000 ids per call and are **metered per 50-id
chunk at 5 credits**, not 5 credits flat. So 200 ids = 4 chunks = **20cr**, and 1000 ids =
20 chunks = **100cr** — against 1000cr fetching them one at a time. Still the single largest
saving available on YouTube, roughly 10×, just not free after the first 50. A request that
resolves nothing returns an empty list at **0 credits**.

### Transcripts: use `/video/subtitles`, not the transcript endpoints

`/video/subtitles` (1cr) returns raw caption files and is **not** a banned endpoint — parse
those yourself.

`/video/transcript` (3cr) and `POST /transcripts` are cleaned transcript endpoints and fall
under the plugin's transcription ban. **The batch one is 3 credits per successful video, not
3 per call** — verified live 08.08.26 (2 ids in, 1 succeeded, `credits_used: 3`), so 100
videos is 300 credits. There is no batch discount here to justify an exemption; captions at
1cr plus the local chain remain strictly cheaper.

Note the ban rules disagree on the batch endpoint: the generator's rule is "path ends
`/transcript`" so `POST /transcripts` is not marked banned in this ref, while
`credit-guard.mjs` tests `transcript` as a substring and does deny it. Treat it as banned.

### `/search/suggestions` is free keyword research

At 1cr, YouTube autocomplete tells you the phrasing real viewers use before you commit a title
or a topic. Cheapest demand signal on the platform.

## Field Maps


The `channel` endpoint returns an `Author` response normalized by the `youtube-author` field map, and `video` returns a `Post` normalized by `youtube-post`. Both include computed fields (`engagement_rate`, `language`, `content_category`, `estimated_reach`) under `data.computed`. Use `?format=raw` to bypass both and get the upstream JSON. Other YouTube endpoints are passthrough.


## Caption files vs transcription (cost note)

`video/subtitles` (1cr) returns the **caption file** — it is NOT a compute-transcript
endpoint and is NOT banned. Legit use: the paid fallback rung in the captions-first
transcription chain when `yt-dlp` is blocked on a machine (e.g. antivirus):
yt-dlp captions (free) → `video/subtitles` (1cr) → Groq → local Whisper.
`video/transcript` (3cr) stays banned — the chain above always wins.
