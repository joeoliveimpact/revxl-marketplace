<!-- GENERATED by tools/socialcrawl/gen_refs.py from research/socialcrawl-docs-2026-07-03/.
     Do NOT hand-edit. Platform wisdom goes in tools/socialcrawl/curated/instagram.md; re-run to merge. -->

# Instagram (33 endpoints)

## Endpoints

| Resource | Params | Credits | Description |
|----------|--------|---------|-------------|
| /profile | `handle` | 1 | Get Instagram user profile |
| /profile/posts | `handle` | 1 | List Instagram user posts |
| /post | `url` | 1 | Get Instagram post details |
| /post/comments | `url` | 5 | List Instagram post comments |
| /comment | `comment_url` / `author_username` / `text_contains` | **5** | Look up one Instagram comment by URL or id |
| /basic-profile | *(none)* | 1 | Get Instagram basic profile |
| /profile/reels | `user_id` / `handle` | 1 | List Instagram user reels |
| /highlights | `user_id` / `handle` | 1 | List Instagram story highlights |
| /highlight/detail | *(none)* | 1 | Get Instagram highlight detail |
| /search/reels | `query` | 1 | Search Instagram reels |
| /media/transcript | `url` | 10 | Get Instagram media transcript — ⛔ **BANNED in this plugin: 10 credits, no advantage. Transcribe with the local captions→Groq→Whisper chain instead.** |
| /user/embed | `handle` | 1 | Get Instagram user embed HTML |
| /audio/reels | `audio_id` | 1 | List Instagram reels using an audio track |
| /search/hashtag | `hashtag` | 5 | Search Instagram posts by hashtag |
| /search/profiles | `query` | 1 | Search Instagram profiles by keyword |
| /reels/trending | *(none)* | 5 | Get trending Instagram reels |
| /followers | `handle` / `user_id` | 5 | List Instagram followers |
| /following | `handle` / `user_id` | 5 | List Instagram following |
| /similar | `handle` / `user_id` | 5 | List similar Instagram accounts |
| /post/likers | `url` | 5 | List Instagram post likers |
| /post/stats | `url` | 5 | Get Instagram post stats including the share count |
| /tagged | `handle` / `user_id` | 5 | List posts an Instagram user is tagged in |
| /location/posts | `location_id` | 5 | List recent posts at an Instagram location |
| /engagement | `handle` | 5 | Get Instagram engagement statistics |
| /search/location | `query` | 5 | Search Instagram locations |
| /username-suggestions | `query` | 5 | Get Instagram username suggestions |
| /search/music | `query` | 5 | Search Instagram music |
| /stories | `handle` / `user_id` | 5 | List an Instagram user's active stories |
| /story/download | `user_id`, `story_id` | 5 | Download a single Instagram story |
| /music/trending | *(none)* | 5 | List trending Instagram music |
| /profile/full | *(none)* | **5** | Instagram profile, recent posts, and computed analytics in one call. |
| /profile/reels/full | `handle` / `user_id` | 5 | Instagram reels with views, likes, comments, and per-reel share counts where available, in one call. |
| /profile/posts/full | `handle` / `user_id` | 5 | Instagram posts with views, likes, comments, and per-post share counts where available, in one call. |

### Parameter legend

- `a`, `b` — both required.
- `a` / `b` — a `oneOf` group: at least one member must be provided.
- *(none)* — no required parameters.

## Parameter Details

- `handle`: Instagram username without the @ symbol
- `trim` *(optional, boolean)*: Set to true to get a trimmed response
- `next_max_id` *(optional, string)*: Cursor to get next page of results.
- `url`: Full URL of the Instagram post
- `region` *(optional, string)*: 2 letter country code to set the proxy in
- `download_media` *(optional, boolean)*: Set to true to also download the video/images and get back permanent, durable media URLs under `data.post.ext.download_media_urls` (`[{ post_id, cdn_url, type, cached }]`). Use these for archiving — the raw `media_urls` are short-lived signed CDN links that expire. Adds a few seconds of latency while the media is fetched.
- `url (/post/comments)`: URL of the Instagram post (a /p/, /reel/, /reels/, or /tv/ link).
- `sort` *(optional, enum: top | recent)*: Ordering: `top` (default) ranks by Instagram's popularity order (most-liked first); `recent` returns newest-first.
- `cursor` *(optional, string)*: Pagination cursor. Use the `next_cursor` from the previous response to fetch the next page.
- `safe_url` *(optional, boolean)*: When true, returns URL-safe profile picture links suitable for embedding.
- `comment_url` *(optional, string)*: An Instagram comment permalink: `https://www.instagram.com/p/{shortcode}/c/{commentId}/` (or a reply permalink `.../c/{parent}/r/{reply}/`, best-effort). Mutually exclusive with `post_url`+`comment_id`.
- `post_url` *(optional, string)*: The post URL (a `/p/`, `/reel/`, `/reels/`, or `/tv/` link). Combine with `comment_id`, or with `author_username`/`text_contains` for a search.
- `comment_id` *(optional, string)*: The target comment's numeric id (`pk`). Requires `post_url`.
- `author_username` *(optional, string)*: Return up to `max` comments authored by this username (no comment id needed). Mutually exclusive with `text_contains` and any comment id.
- `text_contains` *(optional, string)*: Return up to `max` comments whose text contains this snippet (case-insensitive). Mutually exclusive with `author_username` and any comment id.
- `deep_scan` *(optional, boolean)*: Widen the scan budget for deeply-buried comments (raises the per-chain page ceiling and deadline). Bills 15 credits instead of 5.
- `position_hint` *(optional, string)*: Opaque token from a prior lookup's `lookup.position_hint`. Passing it back replays just the last-known sort chain first, making a re-check of an already-found comment cheap.
- `max` *(optional, integer)*: For `author_username`/`text_contains` search: max matches to return (1–20, default 5).
- `userId` *(optional, string)*: Instagram numeric user ID
- `user_id` *(optional, string)*: Instagram user id. Use this for faster response times.
- `max_id` *(optional, string)*: Max id to get more reels. Get 'max_id' from previous response.
- `trim (/profile/reels)` *(optional, boolean)*: Set to true for a trimmed down version of the response
- `user_id (/highlights)` *(optional, string)*: Instagram user id. Use for faster response times.
- `id` *(optional, string)*: Instagram highlight ID — the numeric id, with or without the `highlight:` prefix. Get it from `/v1/instagram/user/highlights`.
- `query`: Search keyword or phrase to find Instagram reels
- `date_posted` *(optional, enum: last-hour | last-day | last-week | last-month | last-year)*: Date posted
- `page` *(optional, integer)*: The page number to return.
- `url (/media/transcript)`: Full URL of the Instagram video or reel
- `audio_id`: Instagram audio ID — the numeric id from an instagram.com/reels/audio/{audio_id}/ URL
- `cursor (/audio/reels)` *(optional, string)*: Pagination cursor from the previous response
- `hashtag`: The hashtag to search for. The leading # is optional.
- `type` *(optional, enum: top | recent | clips)*: Ranking of the returned posts: `top` (default), `recent`, or `clips` (reels only).
- `safe_url (/search/hashtag)` *(optional, boolean)*: When true, returns URL-safe media links suitable for embedding.
- `query (/search/profiles)`: Bio or caption keyword/phrase to search for.
- `cursor (/search/profiles)` *(optional, string)*: The cursor returned by the previous response. In this version it is the next Google results page number.
- `handle (/followers)` *(optional, string)*: Instagram username without the @ symbol.
- `user_id (/followers)` *(optional, string)*: Instagram numeric user ID. Use this for faster responses.
- `handle (/following)` *(optional, string)*: Instagram username without the @ symbol.
- `user_id (/following)` *(optional, string)*: Instagram numeric user ID. Use this for faster responses.
- `handle (/similar)` *(optional, string)*: Instagram username without the @ symbol.
- `user_id (/similar)` *(optional, string)*: Instagram numeric user ID. Use this for faster responses.
- `url (/post/likers)`: URL of the Instagram post (a /p/ or /reel/ link).
- `url (/post/stats)`: URL of the Instagram post (a /p/, /reel/, or /tv/ link).
- `safe_url (/post/stats)` *(optional, boolean)*: When true, returns URL-safe media links suitable for embedding.
- `handle (/tagged)` *(optional, string)*: Instagram username without the @ symbol.
- `user_id (/tagged)` *(optional, string)*: Instagram numeric user ID. Use this for faster responses.
- `safe_url (/tagged)` *(optional, boolean)*: When true, returns URL-safe media links suitable for embedding.
- `location_id`: Instagram numeric location ID.
- `safe_url (/location/posts)` *(optional, boolean)*: When true, returns URL-safe media links suitable for embedding.
- `handle (/engagement)`: Instagram username without the @ symbol.
- `query (/search/location)`: Search keyword or phrase to find Instagram locations.
- `query (/username-suggestions)`: Keyword to seed the username suggestions.
- `query (/search/music)`: Search keyword or phrase to find Instagram audio tracks.
- `handle (/stories)` *(optional, string)*: Instagram username without the @ symbol.
- `user_id (/stories)` *(optional, string)*: Instagram numeric user ID. Use this for faster responses.
- `safe_url (/stories)` *(optional, boolean)*: When true, returns URL-safe media links suitable for embedding.
- `user_id (/story/download)` *(optional, string)*: Instagram numeric user ID of the story's author.
- `story_id` *(optional, string)*: ID of the individual story to download.
- `safe_url (/story/download)` *(optional, boolean)*: When true, returns URL-safe media links suitable for embedding.
- `posts` *(optional, integer)*: How many recent posts to fetch + average the computed metrics over (1–100, default 25).
- `cursor (/profile/full)` *(optional, string)*: Pass a prior response's posts_cursor to deepen the post window.
- `include` *(optional, string)*: CSV subset of posts,computed (default both). include=computed drops the raw posts[] to save payload.
- `handle (/profile/reels/full)` *(optional, string)*: Instagram username (with or without a leading @). Required unless user_id is given. The share-count leg needs a handle; a user_id-only call returns views/likes/comments with shares null (partial refund).
- `user_id (/profile/reels/full)` *(optional, string)*: Numeric Instagram user id. Alternative to handle.
- `cursor (/profile/reels/full)` *(optional, string)*: Opaque cursor from a prior response's next_cursor to page deeper.
- `limit` *(optional, integer)*: Return up to this many items in one call (1–50). The endpoint pages the underlying source server-side until it has collected this many (or runs out), and bills per upstream page consumed (5 credits/page). Omit for a single page.
- `handle (/profile/posts/full)` *(optional, string)*: Instagram username (with or without a leading @). Required unless user_id is given. The share-count leg needs a handle; a user_id-only call returns views/likes/comments with shares null (partial refund).
- `user_id (/profile/posts/full)` *(optional, string)*: Numeric Instagram user id. Alternative to handle.
- `cursor (/profile/posts/full)` *(optional, string)*: Opaque cursor from a prior response's next_cursor to page deeper.

## Pagination (lists)


List endpoints — `profile/posts`, `profile/reels`, `post/comments` — return one page
plus a `next_cursor` in the envelope. To page, pass it back as **`&max_id=<next_cursor>`**
and repeat until `next_cursor` is absent.

```bash
curl -s -H "x-api-key: $SOCIALCRAWL_API_KEY" \
  "https://www.socialcrawl.dev/v1/instagram/profile/reels?handle=instagram&max_id=<next_cursor>"
```

> **Only `max_id` works for IG list paging.** `cursor`, `pagination_token`, and `after`
> are silently ignored — they return page 1 again with no error, so you loop forever on
> the first page. Tested + confirmed; do not use them.

`search/reels` is the exception: it returns a **first-page sample only and does not
paginate** — breadth comes from running many diverse query seeds, not from paging one
search.

## Transient scrape failures (retry, don't hard-fail)


IG scraping can flake upstream. SocialCrawl **auto-refunds** credits on `UPSTREAM_ERROR`
(502) and `SERVICE_UNAVAILABLE` (503), so a failed call costs nothing. On either: retry
once (503 carries a `Retry-After`, default ~30s), and if it still fails, report it and
move on rather than hard-erroring the whole run. An empty/nonexistent profile returns
`RESOURCE_NOT_FOUND` with the credit refunded — treat that as "no data," not a crash.

## Field Maps


The `profile` endpoint returns an `Author` response normalized by the `instagram-author` field map, and `post` returns a `Post` normalized by `instagram-post`. Both include computed fields under `data.computed`. Use `?format=raw` to bypass both. Other Instagram endpoints are passthrough.

## Example

```bash
curl "https://www.socialcrawl.dev/v1/instagram/profile?handle=instagram" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```
