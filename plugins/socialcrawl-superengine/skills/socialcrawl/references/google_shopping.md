<!-- GENERATED by tools/socialcrawl/gen_refs.py from research/socialcrawl-docs-2026-07-03/.
     Do NOT hand-edit. Platform wisdom goes in tools/socialcrawl/curated/google_shopping.md; re-run to merge. -->

# Google Shopping (4 endpoints)

## Endpoints

| Resource | Params | Credits | Description |
|----------|--------|---------|-------------|
| /product-search | `query` | 5 | Search Google Shopping products |
| /product | `product_id` / `gid` / `data_docid` | 1 | Get Google Shopping product detail |
| /reviews | `gid` | 1 | Get Google Shopping product reviews |
| /sellers | `product_id` / `gid` / `data_docid` | 1 | Get Google Shopping sellers for a product |

### Parameter legend

- `a`, `b` — both required.
- `a` / `b` — a `oneOf` group: at least one member must be provided.
- *(none)* — no required parameters.

## Parameter Details

- `query`: Product search keyword (e.g. 'wireless earbuds').
- `country` *(optional, string)*: DataForSEO location name (e.g. 'United States', 'United Kingdom'). Defaults to United States.
- `language` *(optional, string)*: Language code (e.g. 'en'). Defaults to en.
- `depth` *(optional, integer)*: Number of product results to retrieve (default 40, max 120). More results = longer task time.
- `price_min` *(optional, string)*: Minimum product price filter.
- `price_max` *(optional, string)*: Maximum product price filter.
- `sort_by` *(optional, enum: review_score | price_low_to_high | price_high_to_low)*: Result ordering: review_score, price_low_to_high, or price_high_to_low.
- `product_id` *(optional, string)*: Google Shopping product_id (from product-search).
- `gid` *(optional, string)*: Google Shopping gid (from product-search).
- `data_docid` *(optional, string)*: Google Shopping data_docid (from product-search).
- `country (/product)` *(optional, string)*: DataForSEO location name. Defaults to United States.
- `language (/product)` *(optional, string)*: Language code. Defaults to en.
- `gid (/reviews)`: Google Shopping gid of the product (from product-search). Required.
- `product_id (/reviews)` *(optional, string)*: Google Shopping product_id (recommended for accuracy).
- `data_docid (/reviews)` *(optional, string)*: Google Shopping data_docid (recommended for accuracy).
- `depth (/reviews)` *(optional, integer)*: Number of reviews to retrieve (default 10, multiples of 10, max 8000).
- `country (/reviews)` *(optional, string)*: DataForSEO location name. Defaults to United States.
- `language (/reviews)` *(optional, string)*: Language code. Defaults to en.
- `country (/sellers)` *(optional, string)*: DataForSEO location name. Defaults to United States.
- `language (/sellers)` *(optional, string)*: Language code. Defaults to en.

## Example

```bash
curl "https://www.socialcrawl.dev/v1/google_shopping/product-search?query=wireless earbuds" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```
