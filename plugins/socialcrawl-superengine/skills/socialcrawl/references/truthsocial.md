<!-- GENERATED by tools/socialcrawl/gen_refs.py from research/socialcrawl-docs-2026-07-03/.
     Do NOT hand-edit. Platform wisdom goes in tools/socialcrawl/curated/truthsocial.md; re-run to merge. -->

# Truthsocial (3 endpoints)

## Endpoints

| Resource | Params | Credits | Description |
|----------|--------|---------|-------------|
| /profile | `handle` | 1 | Get Truth Social user profile |
| /user/posts | `handle` / `user_id` | 1 | List Truth Social user posts |
| /post | `url` | 1 | Get Truth Social post details |

### Parameter legend

- `a`, `b` — both required.
- `a` / `b` — a `oneOf` group: at least one member must be provided.
- *(none)* — no required parameters.

## Parameter Details

- `handle`: Truth Social username without the @ symbol
- `user_id` *(optional, string)*: Truth Social user id. Use this for faster response times. Trumps is 107780257626128497. It is the 'id' field in the profile endpoint.
- `next_max_id` *(optional, string)*: Used to paginate to next page
- `trim` *(optional, boolean)*: Set to true for a trimmed down version of the response
- `url`: Full URL of the Truth Social post

## Field Maps


The `profile` endpoint returns an `Author` response normalized by the `truthsocial-author` field map, and `post` returns a `Post` normalized by `truthsocial-post`. Both include computed fields under `data.computed`. Use `?format=raw` to bypass both.

## Example

```bash
curl "https://www.socialcrawl.dev/v1/truthsocial/profile?handle=realDonaldTrump" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```
