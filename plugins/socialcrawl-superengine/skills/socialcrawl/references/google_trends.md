<!-- GENERATED by tools/socialcrawl/gen_refs.py from research/socialcrawl-docs-2026-08-08/openapi.json.
     Do NOT hand-edit. Platform wisdom goes in tools/socialcrawl/curated/google_trends.md; re-run to merge. -->

# Google Trends (2 endpoints)

## Endpoints

| Resource | Params | Credits | Description |
|----------|--------|---------|-------------|
| /explore | `keywords` | 5 | Get Google Trends interest over time |
| /rising | `keyword` | 5 | Get related + rising Google Trends queries |

### Parameter legend

- `a`, `b` — both required.
- `a` / `b` — a `oneOf` group: at least one member must be provided.
- `a`? — the spec marks nothing required, but these are the parameters the endpoint accepts;
  a bare call usually will not work. See Endpoint Details for which combination to send.
- *(none)* — takes no parameters at all.
- A method prefix (**POST**, **DELETE**, **PATCH**) marks a non-GET endpoint; unmarked rows are GET.
- Every endpoint also accepts the `Cache-Control` header (send `no-cache` to bypass the
  response cache — billed at the normal rate) and `Idempotency-Key` (a UUID that makes a
  retry safe; a replay returns the cached payload without double-billing).

## Endpoint Details

_What each endpoint returns and when to reach for it._

### /explore — 5cr

Returns Google Trends interest-over-time for up to 5 keywords in one call. Response is `{ series, averages }`: `series` is one entry per keyword, each with dated `points` ({ date, value }) where `value` is Google's 0-100 relative-popularity score; `averages` is the per-keyword mean over the window. Compare terms head-to-head (values are normalised across the keyword set) and scope by `location`, `timeframe`, and `category`. A billed DataForSEO refresh typically takes 3-8s; exact repeats may use the 2-minute search cache and cost 0 credits.

**Parameters**

- `keywords`: 1-5 comma-separated keywords (e.g. 'reverse audio,voice changer'). With multiple keywords the 0-100 values are normalised across the set for direct comparison.
- `location` *(optional)*: Location as an ISO country code ('US', 'GB'), a full name ('United States'), or a numeric DFS code ('2840'). Defaults to worldwide-leaning US.
- `timeframe` *(optional)*: Preset time window: past_hour, past_4_hours, past_day, past_7_days, past_30_days, past_90_days, past_12_months, or past_5_years. Defaults to past_12_months.
- `category` *(optional)*: Numeric Google Trends category code to scope the query (default 0 = all categories).

```bash
curl "https://www.socialcrawl.dev/v1/google_trends/explore?keywords=<keywords>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```

### /rising — 5cr

Returns the related search queries for ONE keyword as `{ rising, top }`. `rising` is the breakout list — queries whose search interest grew the most over the window, each with a `growth` percentage (a true breakout can read into the thousands, e.g. 3200 = +3200%); `top` is the most-searched related queries, each with a 0-100 relative `value`. The closest thing to a 'breakout terms' primitive — pair it with /v1/google_trends/explore to size a trend and find the queries driving it. A billed DataForSEO refresh typically takes 3-8s; exact repeats may use the 2-minute search cache and cost 0 credits.

**Parameters**

- `keyword`: Single keyword to expand (e.g. 'uv index'). Google Trends returns the related-queries list for one keyword only.
- `location` *(optional)*: Location as an ISO country code ('US', 'GB'), a full name ('United States'), or a numeric DFS code ('2840'). Defaults to worldwide-leaning US.
- `timeframe` *(optional)*: Preset time window: past_hour, past_4_hours, past_day, past_7_days, past_30_days, past_90_days, past_12_months, or past_5_years. Defaults to past_12_months.

```bash
curl "https://www.socialcrawl.dev/v1/google_trends/rising?keyword=<keyword>" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```


## Choosing a Google Trends endpoint

| Your goal | Use this | Credits |
|-----------|----------|---------|
| Is interest in this term rising or falling? | `/explore` | 5 |
| What related terms are breaking out? | `/rising` | 5 |

Both 5 credits, and they answer different halves of a trend question. `/explore` gives the
shape over time for a term you already have; `/rising` gives you terms you did not know to
ask about.

### What it's good for

`/rising` (5cr) is the better content-research call of the two: breakout queries are demand
that has not yet been served, which is exactly where a new piece of content or an offer can
land. `/explore` is for validating a term you are already committed to — confirm seasonality
before building around it.

Trends measures *search* demand. For social demand on the same topic, pair with
`reddit/omni-search` (1cr) or `tiktok/search/hashtag` (1cr); for a fused consumer-demand
index across app reviews, web and Reddit, `prism/demand-signals` (30cr) is the one-call
version.
