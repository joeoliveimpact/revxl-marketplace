<!-- GENERATED by tools/socialcrawl/gen_refs.py from research/socialcrawl-docs-2026-07-03/.
     Do NOT hand-edit. Platform wisdom goes in tools/socialcrawl/curated/github.md; re-run to merge. -->

# Github (12 endpoints)

## Endpoints

| Resource | Params | Credits | Description |
|----------|--------|---------|-------------|
| /profile | `handle` | 1 | Get a GitHub user profile |
| /repo | `url` | 1 | Get a GitHub repository |
| /profile/repos | `handle` | 1 | List a GitHub user's repositories |
| /repo/readme | `url` | 1 | Get a repository's README |
| /repo/releases | `url` | 1 | List a repository's releases |
| /repo/issues | `url` | 1 | List a repository's issues (and PRs) |
| /issue | `url` | 1 | Get a single issue or pull request |
| /issue/comments | `url` | 1 | Get comments on an issue or pull request |
| /search | `query` | 1 | Search GitHub issues and pull requests |
| /repo/top-issues | `url` | 5 | Top feature request and top complaint for a repository |
| /repo/dossier | `url` | 5 | Full project dossier for a repository |
| /user/profile-velocity | `handle` | 10 | User contribution velocity dossier |

### Parameter legend

- `a`, `b` — both required.
- `a` / `b` — a `oneOf` group: at least one member must be provided.
- *(none)* — no required parameters.

## Parameter Details

- `handle`: GitHub username — 1–39 chars, alphanumeric + non-consecutive hyphens, no leading/trailing hyphen.
- `url`: GitHub repo URL — `https://github.com/{owner}/{repo}`.
- `handle (/profile/repos)`: GitHub username.
- `type` *(optional, enum: all | owner | member)*: Filter — `all`, `owner`, or `member`. Defaults to `owner`.
- `sort` *(optional, enum: created | updated | pushed | full_name)*: Sort field — `created`, `updated`, `pushed`, or `full_name`. Defaults to `full_name`.
- `direction` *(optional, enum: asc | desc)*: `asc` or `desc`. Defaults to `asc` for full_name, `desc` otherwise.
- `per_page` *(optional, integer)*: Repos per page (1–100). Defaults to 30.
- `page` *(optional, integer)*: 1-indexed page number for pagination.
- `per_page (/repo/releases)` *(optional, integer)*: Releases per page (1–100). Defaults to 30.
- `page (/repo/releases)` *(optional, integer)*: 1-indexed page number.
- `type (/repo/issues)` *(optional, enum: issue | pr | all)*: Filter the unified issues+PRs list — `issue` returns only issues, `pr` returns only pull requests, `all` (default) returns both. Each item also carries `post.ext.type` (`issue`/`pull_request`).
- `state` *(optional, enum: open | closed | all)*: `open`, `closed`, or `all`. Defaults to `open`.
- `labels` *(optional, string)*: Comma-separated label names (e.g. `bug,help wanted`).
- `sort (/repo/issues)` *(optional, enum: created | updated | comments)*: `created`, `updated`, or `comments`. Defaults to `created`.
- `direction (/repo/issues)` *(optional, enum: asc | desc)*: `asc` or `desc`. Defaults to `desc`.
- `since` *(optional, string)*: Only issues updated at or after this ISO 8601 timestamp.
- `per_page (/repo/issues)` *(optional, integer)*: Issues per page (1–100). Defaults to 30.
- `page (/repo/issues)` *(optional, integer)*: 1-indexed page number.
- `url (/issue)`: GitHub HTML URL — `https://github.com/{owner}/{repo}/issues/{n}` or `/pull/{n}`.
- `url (/issue/comments)`: GitHub HTML URL of the issue or PR.
- `sort (/issue/comments)` *(optional, enum: created | updated)*: `created` or `updated`. Defaults to `created`.
- `direction (/issue/comments)` *(optional, enum: asc | desc)*: `asc` or `desc`. Defaults to `asc`.
- `since (/issue/comments)` *(optional, string)*: Only comments updated at or after this ISO 8601 timestamp.
- `per_page (/issue/comments)` *(optional, integer)*: Comments per page (1–100). Defaults to 30.
- `page (/issue/comments)` *(optional, integer)*: 1-indexed page number.
- `query`: GitHub search query (uses GitHub's qualifier syntax — see https://docs.github.com/en/search-github/searching-on-github/searching-issues-and-pull-requests).
- `sort (/search)` *(optional, enum: reactions | comments | created | updated)*: Sort field — `reactions`, `comments`, `created`, `updated`. Defaults to best-match relevance.
- `order` *(optional, enum: asc | desc)*: `asc` or `desc`. Defaults to `desc`.
- `per_page (/search)` *(optional, integer)*: Results per page (1–100). Defaults to 30.
- `page (/search)` *(optional, integer)*: 1-indexed page number.
- `handle (/user/profile-velocity)`: GitHub username.
- `depth` *(optional, enum: quick | default | deep)*: `quick`, `default`, or `deep`. Defaults to `default`. Trades off upstream calls vs. dossier richness.

## Field Maps


- `profile` → `Author` via the `github-author` field map (followers, following, public_repos → likes_count, etc.). Includes computed fields under `data.computed`.
- `repo` → `Author` via the `github-repo` field map (stargazers_count → followers, forks_count → following, subscribers_count → likes_count). Includes computed fields.
- `issue` → `Post` via the `github-post` field map (title → content.text, reactions.total_count → engagement.likes, comments → engagement.comments).
- All other endpoints are passthrough — `repo/readme` returns the raw markdown string, the rest pass GitHub's native JSON.
- Use `?format=raw` to bypass field-mapping on the three field-mapped endpoints.

## Composite endpoints


The 3 composite endpoints fan out multiple GitHub calls server-side and fold the results into a single dossier:

- **`repo/top-issues`** (~3 calls) — `{ top_feature_request, top_complaint }`. Either can be `null`. If both are null, returns 404 with credit refunded.
- **`repo/dossier`** (~5 calls in parallel) — `{ info, readme, releases, top_issues }`. The `info` call is critical (404 → refund); other enrichments degrade gracefully.
- **`user/profile-velocity`** (~5–15 calls) — `{ velocity, contributed_repos, own_repos }`. Three depth modes via the `depth` optional param: `quick` (3 own + 5 external), `default` (5 + 10), `deep` (5 + 15). `deep` can take 2–3 seconds wall-clock.

## Rate limits


The shared service token caps at 5,000 req/hr for general endpoints and 30 req/min for search. Cache TTLs (profile 15min, post 10min, comments 5min, search 2min, analytics 30min) absorb most of it. Cache hits and 4xx/5xx refunds keep customers off the hook when the bucket is squeezed.

## Example

```bash
curl "https://www.socialcrawl.dev/v1/github/profile?handle=octocat" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```
