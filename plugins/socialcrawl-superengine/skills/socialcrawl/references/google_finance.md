<!-- GENERATED by tools/socialcrawl/gen_refs.py from research/socialcrawl-docs-2026-07-03/.
     Do NOT hand-edit. Platform wisdom goes in tools/socialcrawl/curated/google_finance.md; re-run to merge. -->

# Google Finance (3 endpoints)

## Endpoints

| Resource | Params | Credits | Description |
|----------|--------|---------|-------------|
| /quote | `keyword` | 5 | Get a financial instrument quote |
| /ticker-search | `keyword` | 1 | Search financial instruments by name |
| /markets | *(none)* | 1 | Get a markets overview (indices + movers) |

### Parameter legend

- `a`, `b` — both required.
- `a` / `b` — a `oneOf` group: at least one member must be provided.
- *(none)* — no required parameters.

## Parameter Details

- `keyword`: Instrument identifier: TICKER:EXCHANGE for stocks/ETFs/indices ('GOOGL:NASDAQ', '.INX:INDEXSP') or a forex/crypto pair ('EUR-USD', 'BTC-USD'). Use the `id` returned by ticker-search.
- `language` *(optional, string)*: Language as a DFS name ('English') or 2-letter code ('en'). Defaults to English.
- `location` *(optional, string)*: Location as a DFS name ('United States') or numeric code ('2840'). Defaults to the US.
- `keyword (/ticker-search)`: Company / instrument name to search for (e.g. 'Apple', 'Euro', 'Bitcoin').
- `category` *(optional, enum: all | stock | index | mutual_fund | currency | futures)*: Restrict to one instrument class: all (default), stock, index, mutual_fund, currency, or futures. A class with no matches returns an empty list.

## Example

```bash
curl "https://www.socialcrawl.dev/v1/google_finance/quote?keyword=GOOGL:NASDAQ" \
  -H "x-api-key: $SOCIALCRAWL_API_KEY"
```
