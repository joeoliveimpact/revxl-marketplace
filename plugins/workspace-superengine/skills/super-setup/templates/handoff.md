# handoff.md — {{WORKSPACE_NAME}}

What the next session should do first. Rewritten in full by `/session-closeout` at the end of every session.

---

## P0 (do first)

- [ ] Read `CLAUDE.md` if you haven't this session (the override constraints load automatically from `.claude/rules/overrides.md`).
- [ ] Confirm the scaffold matches your real intent — edit `GOALS.md` and `ARCHITECTURE.md` if anything is off.
- [ ] Add your first real project row to `PLANNING.md`.

## P1 (do soon)

- [ ] Inventory existing assets (files, integrations, prior notes) and reflect them in `ARCHITECTURE.md` + `MEMORY.md`.
- [ ] Decide whether you need any modules (e.g. `code`, `client-work`, `content`) — wait for v0.2 module support.

## P2 (when time)

- [ ] Set workspace verbosity in `.claude/workspace.yml` if the default doesn't fit.

## Blockers

- _none yet_

## Notes for next session

- This workspace was scaffolded fresh on {{DATE}} — there is no prior context to load yet.

_Generated by `/super-setup` on {{DATE}}. Will be overwritten by `/session-closeout`._
