# tasks/findings.md — {{WORKSPACE_NAME}}

Append-only discovery log. When research or debugging surfaces something worth remembering for later — but it isn't yet a `decisions` entry in `MEMORY.md` — drop it here. Newest on top.

Format per entry:

```
## {{DATE}} — short title
**Context:** what you were doing
**Finding:** what you learned
**Implication:** what changes because of it (or "TBD")
```

---

<!-- entries below -->
